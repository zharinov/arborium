[package]
name = "<%= crate_name %>"
version = "<%= workspace_version %>"
edition = "2024"
rust-version = "1.85"
description = "<%= grammar_name %> grammar for arborium (tree-sitter bindings)"
authors = ["Amos Wenger <amos@bearcove.eu>"]
license = "<%= license %>"
readme = "README.md"
repository = "https://github.com/bearcove/arborium"
homepage = "https://github.com/bearcove/arborium"
documentation = "https://docs.rs/<%= crate_name %>"
keywords = ["tree-sitter", "<%= grammar_id %>", "syntax-highlighting", "<%= tag %>"]
categories = ["parsing", "text-processing"]

[lib]
path = "src/lib.rs"

[dependencies]
tree-sitter-patched-arborium = { version = "<%= workspace_version %>", path = "<%= repo_rel %>/tree-sitter" }
arborium-sysroot = { version = "<%= workspace_version %>", path = "<%= shared_rel %>/arborium-sysroot" }
<% for dep in highlights_prepend_deps { %>
<%= dep.crate_name %> = { version = "<%= dep.version %>", path = "<%= dep.rel_path %>" }
<% } %>

[dev-dependencies]
arborium-test-harness = { version = "<%= workspace_version %>", path = "<%= shared_rel %>/arborium-test-harness" }

[build-dependencies]
cc = "1"
